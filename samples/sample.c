#include <stdio.h>
#include "not_existant.c"

int test(void);

int g;

int main(int t, char* args) {
    smile(t, g);
    t = g;
    int *asd;
    t = args[g];
    for(int i; i < 5; i++) {
        printf("i is %d", i);
    }
}
